<% if @idea.present? %>
  <div class="refresh-btn text-center">
    <%= link_to  random_path, class: 'btn btn-primary' do %>
      <i class="fa fa-refresh" aria-hidden="true"></i> Inspire Me 
    <% end %>
  </div>
  <div class="panel panel-default">
    <div class="panel-body">
      <blockquote>
        <p><%= @idea.description %></p>
        <small>by <%= @idea.author %></small>
      </blockquote>
        
      <br>

      <div class="btn-group" role="group">
        <%= link_to  idea_path(@idea), method: :delete, class: 'btn btn-sm btn-danger', data: {confirm: "Are you sure?"} do %>
          <i class="fa fa-trash-o" aria-hidden="true"></i>
        <% end %>
        <%= link_to  edit_idea_path(@idea), class: 'btn btn-sm btn-success' do  %>
          <i class="fa fa-pencil-square" aria-hidden="true"></i>
        <% end %>
      </div>
    </div>
  </div>
  <% else %>
  <h4>No ideas have been shared. Add one <%= link_to 'here', root_path %></h4>

<% end %>